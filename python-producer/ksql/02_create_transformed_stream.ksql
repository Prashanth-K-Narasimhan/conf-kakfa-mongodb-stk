CREATE STREAM telemetry_raw WITH (
  KAFKA_TOPIC='telemetry_raw',
  VALUE_FORMAT='JSON'
) AS
SELECT
  did,
  timestamp,
  name,
  int_value,
  float_value,
  string_value,
  eventTime,
  receivedTime,
  ingest_rowtime
FROM telemetry_stream
EMIT CHANGES;
