CREATE STREAM TELEMETRY_RAW
WITH (
    KAFKA_TOPIC='telemetry_raw',
    VALUE_FORMAT='JSON'
) AS
SELECT
    DID,
    TIMESTAMP,
    NAME,
    VALUE_TYPE,
    FLOAT_VALUE,
    STRING_VALUE,
    INT_VALUE,
    EVENTTIME,
    RECEIVEDTIME
FROM TELEMETRY_STREAM
EMIT CHANGES;
