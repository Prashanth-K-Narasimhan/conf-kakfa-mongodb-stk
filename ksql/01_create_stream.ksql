CREATE STREAM TELEMETRY_STREAM (
    DID STRING,
    TIMESTAMP BIGINT,
    NAME STRING,
    VALUE_TYPE STRING,
    FLOAT_VALUE DOUBLE,
    STRING_VALUE STRING,
    INT_VALUE INTEGER,
    EVENTTIME STRING,
    RECEIVEDTIME STRING
) WITH (
    KAFKA_TOPIC='telemetry',
    VALUE_FORMAT='JSON'
);
